[project]
name = "touchline-web"
version = "0.1.0"
dependencies = [ "fastapi>=0.115, <1", "uvicorn>=0.34, <1", "numpy>=2.2, <3", "scipy>=1.15, <2", "httpx>=0.28, <1", "pytest>=8, <10", "psycopg[binary]>=3.2, <4", "understatapi==0.7.1" ]
requires-python = ">=3.12,<3.13"
